## ASP.NET Core Web API App

Scaffolded as `dotnet new webapi` and moved the weather code to it's own folder.

## Run

```bash
dotnet run
```

Now you can open [localhost:5000/docs](http://localhost:5000/docs) and check out the API endpoints.
